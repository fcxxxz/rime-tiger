# Rime schema: tigress
# encoding: utf-8
#
# 本文件是“词库 · 常用字”方案。
#
# 配置来源：
#   tiger_base.schema.yaml -> tigress_full.schema.yaml -> tigress.schema.yaml
#
# tigress_full 是全字集父配置，并启用词库版加词/删词/调序。
# 本文件继承后只把名称和词典覆盖成常用字。
#
# 修改指南：
#   1. 只改词库常用：改本文件 / tigress.custom.yaml。
#   2. 词库全字集和词库常用都要改：改 tigress_full.schema.yaml /
#      tigress_full.custom.yaml。
#   3. 四个方案都要改：改 tiger_base.schema.yaml / tiger_base.custom.yaml。

__include: tigress_full.schema:/

schema:
  description: |
    虎码官方词库
    字集：9767 字
    敲 ` 鍵進入拼音反查
  name: "虎码词库 9767字"
  schema_id: tigress

translator:
  dictionary: tigress.extended
