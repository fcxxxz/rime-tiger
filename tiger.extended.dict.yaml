# Rime dictionary: tiger.extended
# encoding: utf-8

---
name: tiger.extended
version: "2026.06.22"
sort: by_weight
use_preset_vocabulary: false
import_tables:
  - tiger.common
  - tiger.user

columns:
  - text
  - code
  - weight
  - stem
encoder:
  rules:
    - length_equal: 2
      formula: "AaAbBaBb"
    - length_equal: 3
      formula: "AaBaCaCb"
    - length_in_range: [4, 99]
      formula: "AaBaCaZa"
...

# User entries are stored in the imported *.user dictionary.
