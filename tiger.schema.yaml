# Rime schema: tiger
# encoding: utf-8
#
# 本文件是“单字 · 常用字”方案。
#
# 配置来源：
#   tiger_base.schema.yaml -> tiger_full.schema.yaml -> tiger.schema.yaml
#
# tiger_full 是全字集父配置；本文件只把名称和词典覆盖成常用字。
#
# 修改指南：
#   1. 只改单字常用：改本文件 / tiger.custom.yaml。
#   2. 单字全字集和单字常用都要改：改 tiger_full.schema.yaml /
#      tiger_full.custom.yaml。
#   3. 四个方案都要改：改 tiger_base.schema.yaml / tiger_base.custom.yaml。

__include: tiger_full.schema:/

schema:
  description: |
    虎码官方单字
    字集：9767 字
    敲 ` 鍵進入拼音反查
  name: "虎码单字 9767字"
  schema_id: tiger

translator:
  dictionary: tiger.extended
